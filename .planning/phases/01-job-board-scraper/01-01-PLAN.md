---
phase: 01-job-board-scraper
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/scrapers/types.ts, src/scrapers/base/BaseScraper.ts, src/scrapers/utils/rateLimiter.ts]
autonomous: true
requirements: [JOB-01, JOB-02, JOB-03]
must_haves:
  truths:
    - "Job interface defines standard fields (title, company, location, link, description, source, scrapedAt)"
    - "BaseScraper provides reusable extraction logic"
    - "Rate limiter enforces 5-10s delays with jitter"
  artifacts:
    - path: "src/scrapers/types.ts"
      provides: "Job interface and scraper types"
      min_lines: 40
    - path: "src/scrapers/base/BaseScraper.ts"
      provides: "Abstract base class with common methods"
      min_lines: 60
    - path: "src/scrapers/utils/rateLimiter.ts"
      provides: "Rate limiting with jitter"
      min_lines: 30
  key_links:
    - from: "src/scrapers/base/BaseScraper.ts"
      to: "src/scrapers/types.ts"
      via: "imports Job interface"
      pattern: "import.*Job.*from.*types"
    - from: "src/scrapers/utils/rateLimiter.ts"
      to: "src/scrapers/base/BaseScraper.ts"
      via: "used by scrapers"
      pattern: "import.*rateLimiter"
---

<objective>
Create the foundation for job board scraping: types, base class, and rate limiting.

Purpose: Establish contracts and reusable infrastructure that all scrapers will use. This prevents duplication and ensures consistent job data structure across all sources.

Output: TypeScript types, abstract base scraper, rate limiter utility
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-job-board-scraper/01-RESEARCH.md
</context>

<interfaces>
<!-- These types will be created by this plan and used by subsequent scraper plans -->
<!-- Extract from codebase after creation -->
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Define Job interface and scraper types</name>
<files>src/scrapers/types.ts</files>
<action>Create TypeScript types for job scraping:
- Job interface with fields: id, title, company, location, link, description, source, scrapedAt
- ScraperConfig interface for configuration options
- ScraperResult type for return values
- Export all types for use by scraper implementations

Use research recommendations: 7 core fields plus metadata (source, scrapedAt).
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/types.ts</automated>
</verify>
<done>Type definitions compile without errors, all types exported</done>
</task>

<task type="auto">
<name>Task 2: Create abstract BaseScraper class</name>
<files>src/scrapers/base/BaseScraper.ts</files>
<action>Create abstract base class providing:
- Abstract scrape() method that subclasses must implement
- Common validateJob() method to check required fields
- Common normalizeJob() method for standardizing extracted data
- Logger instance for consistent logging
- Constructor accepting scraper configuration

Base should enforce the Job interface contract and provide reusable validation logic.
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/base/BaseScraper.ts</automated>
</verify>
<done>BaseScraper compiles, extends Job type, provides abstract scrape() method</done>
</task>

<task type="auto">
<name>Task 3: Implement rate limiter with jitter</name>
<files>src/scrapers/utils/rateLimiter.ts</files>
<action>Create RateLimiter class with:
- Configurable baseDelay (default 5000ms) and jitter (default 5000ms)
- wait() method returning Promise that resolves after random delay
- Formula: delay = baseDelay + Math.random() * jitter (5-10s range)
- Console logging for transparency

Per research: prevents detection by avoiding predictable request patterns.
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/utils/rateLimiter.ts</automated>
</verify>
<done>RateLimiter compiles, exports class with wait() method</done>
</task>
</tasks>

<verification>
All foundation files compile without TypeScript errors. BaseScraper properly imports Job type from types.ts. RateLimiter is ready for import by scraper implementations.
</verification>

<success_criteria>
1. TypeScript compilation passes for all three files
2. Job interface has all 8 required fields
3. BaseScraper is abstract with abstract scrape() method
4. RateLimiter wait() returns Promise<void>
5. All files use ES module exports
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-board-scraper/01-01-SUMMARY.md`
</output>
