---
phase: 01-job-board-scraper
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/scrapers/strategies/puppeteerScraper.ts, src/scrapers/strategies/linkedin.ts]
autonomous: true
requirements: [JOB-01]
must_haves:
  truths:
    - "LinkedIn scraper uses Puppeteer for headless Chrome automation"
    - "Uses puppeteer-extra-plugin-stealth for anti-detection"
    - "Waits for network idle and job selectors"
    - "Extracts jobs from LinkedIn job search results"
  artifacts:
    - path: "src/scrapers/strategies/puppeteerScraper.ts"
      provides: "Puppeteer-based scraper for JS-heavy sites"
      min_lines: 70
    - path: "src/scrapers/strategies/linkedin.ts"
      provides: "LinkedIn.com implementation"
      min_lines: 60
  key_links:
    - from: "src/scrapers/strategies/linkedin.ts"
      to: "src/scrapers/strategies/puppeteerScraper.ts"
      via: "extends PuppeteerScraper"
      pattern: "extends PuppeteerScraper"
    - from: "src/scrapers/strategies/puppeteerScraper.ts"
      to: "src/scrapers/base/BaseScraper.ts"
      via: "extends BaseScraper"
      pattern: "extends BaseScraper"
---

<objective>
Implement LinkedIn.com job scraper using Puppeteer with stealth.

Purpose: LinkedIn requires headless Chrome automation due to heavy JavaScript rendering. This scraper uses Puppeteer with stealth plugin to avoid detection.

Output: Working LinkedIn scraper with Puppeteer strategy
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-job-board-scraper/01-RESEARCH.md
@.planning/phases/01-job-board-scraper/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 -->
<!-- import { Job, ScraperConfig } from '../types'; -->
<!-- import { BaseScraper } from '../base/BaseScraper'; -->
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Create Puppeteer scraper strategy</name>
<files>src/scrapers/strategies/puppeteerScraper.ts</files>
<action>Create PuppeteerScraper class extending BaseScraper:
- Uses puppeteer-extra with StealthPlugin
- Launches headless Chrome with GPU acceleration
- Implements abstract scrape() method
- Sets realistic viewport (1920x1080)
- Waits for network idle and specific selectors
- Extracts jobs via page.evaluate()
- Properly closes browser and pages in finally block

Per research: Always use stealth plugin, close resources, handle memory leaks.
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/strategies/puppeteerScraper.ts</automated>
</verify>
<done>PuppeteerScraper compiles, extends BaseScraper, uses stealth</done>
</task>

<task type="auto">
<name>Task 2: Implement LinkedIn.com scraper</name>
<files>src/scrapers/strategies/linkedin.ts</files>
<action>Create LinkedIn scraper extending PuppeteerScraper:
- Target URL: https://linkedin.com/jobs/search?q={query}
- Selectors: .job-card-list__item, .job-card-list__title, .job-card-container__company-name
- Wait for .job-card-list__item selector with timeout
- Extract: title, company, location, link
- Handle empty results gracefully

Per research: LinkedIn is high difficulty, requires stealth, aggressive anti-bot.
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/strategies/linkedin.ts</automated>
</verify>
<done>LinkedIn scraper compiles, extends PuppeteerScraper, targets linkedin.com</done>
</task>
</tasks>

<verification>
Both Puppeteer-based files compile without TypeScript errors. LinkedIn scraper properly targets LinkedIn job search and extracts required fields.
</verification>

<success_criteria>
1. TypeScript compilation passes for both files
2. PuppeteerScraper uses puppeteer-extra with StealthPlugin
3. LinkedIn scraper has correct selectors for LinkedIn jobs
4. Browser resources are properly closed in finally block
5. Jobs extracted include all required fields
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-board-scraper/01-03-SUMMARY.md`
</output>
