---
phase: 01-job-board-scraper
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/scrapers/strategies/indeed.ts, src/scrapers/strategies/httpScraper.ts]
autonomous: true
requirements: [JOB-02]
must_haves:
  truths:
    - "Indeed scraper extracts job listings from Indeed.com"
    - "Uses Axios for HTTP requests with rotating user-agents"
    - "Uses Cheerio for HTML parsing"
    - "Respects rate limiting between requests"
  artifacts:
    - path: "src/scrapers/strategies/httpScraper.ts"
      provides: "HTTP-based scraper using Cheerio"
      min_lines: 50
    - path: "src/scrapers/strategies/indeed.ts"
      provides: "Indeed.com implementation"
      min_lines: 60
  key_links:
    - from: "src/scrapers/strategies/indeed.ts"
      to: "src/scrapers/strategies/httpScraper.ts"
      via: "extends HttpScraper"
      pattern: "extends HttpScraper"
    - from: "src/scrapers/strategies/httpScraper.ts"
      to: "src/scrapers/base/BaseScraper.ts"
      via: "extends BaseScraper"
      pattern: "extends BaseScraper"
---

<objective>
Implement Indeed.com job scraper using Axios and Cheerio.

Purpose: Create the simplest scraper first to validate the pattern. Indeed is chosen as the starting point because it has medium difficulty and may work with HTTP scraping (per research).

Output: Working Indeed scraper with HTTP+Cheerio strategy
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-job-board-scraper/01-RESEARCH.md
@.planning/phases/01-job-board-scraper/01-01-SUMMARY.md
</context>

<interfaces>
<!-- From Plan 01 -->
<!-- import { Job, ScraperConfig } from '../types'; -->
<!-- import { BaseScraper } from '../base/BaseScraper'; -->
<!-- import { rateLimiter } from '../utils/rateLimiter'; -->
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Create HTTP scraper strategy</name>
<files>src/scrapers/strategies/httpScraper.ts</files>
<action>Create HttpScraper class extending BaseScraper:
- Uses Axios for HTTP requests
- Uses Cheerio for HTML parsing
- Implements abstract scrape() method
- Accepts search query parameter
- Handles user-agent rotation
- Returns Job[] array

This is the base HTTP strategy that Indeed will use. Include fallback selectors for resilience.
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/strategies/httpScraper.ts</automated>
</verify>
<done>HttpScraper compiles, extends BaseScraper, implements scrape() method</done>
</task>

<task type="auto">
<name>Task 2: Implement Indeed.com scraper</name>
<files>src/scrapers/strategies/indeed.ts</files>
<action>Create Indeed scraper extending HttpScraper:
- Target URL: https://indeed.com/jobs?q={query}
- Selectors: div[data-testid="result"], h2 a span (title), span[data-testid="company-name"], div[data-testid="text-location"]
- Extract: title, company, location, link
- Normalize location format
- Handle relative URLs (prepend https://indeed.com)

Per research: Indeed is good starting point, may need CAPTCHA handling.
</action>
<verify>
<automated>npx tsc --noEmit src/scrapers/strategies/indeed.ts</automated>
</verify>
<done>Indeed scraper compiles, extends HttpScraper, targets indeed.com</done>
</task>
</tasks>

<verification>
Both scraper files compile without TypeScript errors. Indeed scraper properly extends HttpScraper and implements all required Job fields.
</verification>

<success_criteria>
1. TypeScript compilation passes for both files
2. HttpScraper uses Axios and Cheerio
3. Indeed scraper has correct selectors for Indeed.com
4. Jobs extracted include all required fields
5. Rate limiting is applied between requests
</success_criteria>

<output>
After completion, create `.planning/phases/01-job-board-scraper/01-02-SUMMARY.md`
</output>
