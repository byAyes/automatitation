---
phase: 05-pdf-job-detection
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified: [src/lib/pdf/pdfIntegration.ts, src/app/api/pdf/match/route.ts]
autonomous: true
requirements: [PDF-03, PDF-04]
must_haves:
  truths:
    - "Extracted jobs are scored against user profile"
    - "Matched jobs from PDFs included in email digests"
    - "Integration uses existing scoring system"
    - "PDF jobs flow into email workflow"
  artifacts:
    - path: "src/lib/pdf/pdfIntegration.ts"
      provides: "Integration logic for PDF jobs with matching/email"
      min_lines: 60
    - path: "src/app/api/pdf/match/route.ts"
      provides: "API endpoint for matching PDF jobs"
      min_lines: 70
  key_links:
    - from: "src/lib/pdf/pdfIntegration.ts"
      to: "src/matching/scorer.ts"
      via: "imports calculateMatchScore"
      pattern: "import.*calculateMatchScore"
    - from: "src/lib/pdf/pdfIntegration.ts"
      to: "src/lib/email/template.ts"
      via: "imports formatJobDigest"
      pattern: "import.*formatJobDigest"
---

<objective>
Integrate PDF-extracted jobs with existing job matching algorithm and email notification workflow.

Purpose: Ensure jobs extracted from PDFs use the same matching system as scraped jobs and are included in weekly email digests. This completes the PDF-to-email pipeline.

Output: Integration utilities, API endpoint for matching PDF jobs
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-pdf-job-detection/05-CONTEXT.md
@src/matching/scorer.ts
@src/lib/email/template.ts
@src/app/api/match-jobs/route.ts
</context>

<interfaces>
<!-- Key types from existing codebase -->
<!-- From src/matching/scorer.ts: calculateMatchScore, scoreAndSortJobs -->
<!-- From src/lib/email/template.ts: formatJobDigest -->
<!-- From src/types/job.ts: Job interface -->
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Create PDF integration utilities</name>
<files>src/lib/pdf/pdfIntegration.ts</files>
<action>Create integration logic to connect PDF jobs with existing systems:
- Export function matchPDFJobs(jobs: ExtractedJob[], userId: string, threshold?: number): Promise<MatchedJob[]>
  * Fetch user profile from Prisma
  * Convert ExtractedJob[] to Job[] format (add default fields)
  * Call scoreAndSortJobs() from existing scorer
  * Return matched jobs above threshold
- Export function preparePDFJobsForEmail(jobs: MatchedJob[]): Promise<void>
  * Group PDF jobs by email digest
  * Mark jobs with emailDigestId
  * Integrate with existing EmailDigest workflow

Leverage infrastructure: Use existing scoreAndSortJobs from scorer.ts and email digest system.
</action>
<verify>
<automated>npx tsc --noEmit src/lib/pdf/pdfIntegration.ts</automated>
</verify>
<done>Integration utilities compile, export matchPDFJobs and preparePDFJobsForEmail functions</done>
</task>

<task type="auto">
<name>Task 2: Create PDF job matching API endpoint</name>
<files>src/app/api/pdf/match/route.ts</files>
<action>Create Next.js API route for matching PDF jobs:
- Export GET function accepting userId and threshold params
- Steps:
  1. Validate userId parameter
  2. Fetch user profile from Prisma
  3. Get all PDF-sourced jobs from database (filter by source='pdf' or similar marker)
  4. Call scoreAndSortJobs() with PDF jobs and user profile
  5. Return { matches: MatchedJob[], total: number, threshold }
- Support optional PDF file upload (POST) for on-demand matching:
  1. Parse PDF from request
  2. Extract jobs
  3. Score immediately
  4. Return matches without saving

Per PDF-03/PDF-04: Integration with matching system and email workflow.
</action>
<verify>
<automated>npx tsc --noEmit src/app/api/pdf/match/route.ts</automated>
</verify>
<done>Matching endpoint compiles, exports GET handler, returns scored PDF jobs</done>
</task>
</tasks>

<verification>
Integration utilities compile without TypeScript errors. PDF jobs are scored using the same algorithm as scraped jobs. Matched PDF jobs can be included in email digests. API endpoint returns properly formatted match results.
</verification>

<success_criteria>
1. TypeScript compilation passes for both files
2. matchPDFJobs function uses existing scoreAndSortJobs from scorer.ts
3. preparePDFJobsForEmail integrates with EmailDigest workflow
4. GET /api/pdf/match accepts userId and returns matched PDF jobs
5. PDF jobs use same scoring as scraped jobs (consistent matching)
6. All files use ES module exports
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-job-detection/05-03-SUMMARY.md`
</output>
