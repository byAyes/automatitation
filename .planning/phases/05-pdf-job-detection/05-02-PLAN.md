---
phase: 05-pdf-job-detection
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [src/app/api/pdf/upload/route.ts, src/lib/pdf/duplicateDetector.ts]
autonomous: true
requirements: [PDF-01, PDF-05]
must_haves:
  truths:
    - "API endpoint accepts PDF file uploads"
    - "PDF is parsed and jobs are extracted"
    - "Duplicate detection prevents same job appearing twice"
    - "Extracted jobs are saved to database"
  artifacts:
    - path: "src/app/api/pdf/upload/route.ts"
      provides: "POST endpoint for PDF upload"
      min_lines: 80
    - path: "src/lib/pdf/duplicateDetector.ts"
      provides: "Duplicate detection logic"
      min_lines: 50
  key_links:
    - from: "src/app/api/pdf/upload/route.ts"
      to: "src/lib/pdf/pdfParser.ts"
      via: "imports parsePDF"
      pattern: "import.*parsePDF"
    - from: "src/app/api/pdf/upload/route.ts"
      to: "src/lib/pdf/jobExtractor.ts"
      via: "imports extractJobsFromText"
      pattern: "import.*extractJobsFromText"
    - from: "src/lib/pdf/duplicateDetector.ts"
      to: "prisma schema"
      via: "queries Job table"
      pattern: "prisma\\.job\\.find"
---

<objective>
Create API endpoint for PDF upload and implement duplicate detection to prevent jobs from appearing multiple times.

Purpose: Provide users with a way to upload PDFs via API and ensure extracted jobs don't duplicate existing scraped jobs. This connects PDF parsing to the database while preventing duplicates.

Output: POST /api/pdf/upload endpoint, duplicate detection utility
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-pdf-job-detection/05-CONTEXT.md
@src/app/api/match-jobs/route.ts
@prisma/schema.prisma
</context>

<interfaces>
<!-- Key types from existing codebase -->
<!-- From src/types/job.ts: Job interface -->
<!-- From prisma/schema.prisma: Job model with url unique constraint -->
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Implement duplicate detection logic</name>
<files>src/lib/pdf/duplicateDetector.ts</files>
<action>Create duplicate detection utility:
- Export async function isDuplicate(job: ExtractedJob): Promise<boolean>
- Check against existing jobs in database using multiple strategies:
  * URL match (exact match on url field)
  * Title + Company similarity (fuzzy match on title + company combination)
  * Normalize titles (lowercase, remove special chars) for comparison
- Return true if job already exists, false otherwise
- Use Prisma client to query Job table

Per PDF-05 requirement: Prevent same job appearing from PDF and scraping sources.
</action>
<verify>
<automated>npx tsc --noEmit src/lib/pdf/duplicateDetector.ts</automated>
</verify>
<done>Duplicate detector compiles, exports isDuplicate function, queries Job table</done>
</task>

<task type="auto">
<name>Task 2: Create PDF upload API endpoint</name>
<files>src/app/api/pdf/upload/route.ts</files>
<action>Create Next.js API route handler:
- Export POST function handling multipart/form-data or binary PDF upload
- Steps:
  1. Parse request body (Buffer from PDF file)
  2. Call parsePDF(buffer) to extract text
  3. Call extractJobsFromText(text) to get job candidates
  4. For each candidate:
     - Check isDuplicate(candidate)
     - If not duplicate: convert to Job format and save to Prisma
  5. Return { success: true, jobsAdded: count, duplicates: count }
- Handle errors gracefully (invalid PDF, extraction failures)
- Use existing Job Prisma model for persistence

Leverage infrastructure: Use existing Prisma schema (Job model) from 05-CONTEXT.md.
</action>
<verify>
<automated>npx tsc --noEmit src/app/api/pdf/upload/route.ts</automated>
</verify>
<done>Upload endpoint compiles, exports POST handler, saves non-duplicate jobs</done>
</task>
</tasks>

<verification>
API endpoint compiles without TypeScript errors. Duplicate detection prevents same job from being added twice. PDF upload successfully saves extracted jobs to database. Endpoint returns proper response with job counts.
</verification>

<success_criteria>
1. TypeScript compilation passes for both files
2. isDuplicate function checks URL and title+company similarity
3. POST endpoint accepts PDF Buffer and returns JSON response
4. Non-duplicate jobs are saved to Prisma Job table
5. Duplicate jobs are counted but not saved
6. Error handling for invalid PDFs and extraction failures
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-job-detection/05-02-SUMMARY.md`
</output>

</output>
