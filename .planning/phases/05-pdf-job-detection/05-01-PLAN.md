---
phase: 05-pdf-job-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/lib/pdf/pdfParser.ts, src/lib/pdf/jobExtractor.ts, src/types/pdf.ts]
autonomous: true
requirements: [PDF-01, PDF-02]
must_haves:
  truths:
    - "User can upload PDF via API endpoint"
    - "System extracts text content from uploaded PDF"
    - "Individual job postings are identified and structured"
    - "Extracted data includes title, company, description, requirements"
  artifacts:
    - path: "src/lib/pdf/pdfParser.ts"
      provides: "PDF parsing with pdf-parse library"
      min_lines: 40
    - path: "src/lib/pdf/jobExtractor.ts"
      provides: "Job posting extraction from text"
      min_lines: 80
    - path: "src/types/pdf.ts"
      provides: "PDF upload and extraction types"
      min_lines: 30
  key_links:
    - from: "src/lib/pdf/jobExtractor.ts"
      to: "src/types/job.ts"
      via: "imports Job interface"
      pattern: "import.*Job.*from.*types/job"
    - from: "src/lib/pdf/pdfParser.ts"
      to: "src/lib/pdf/jobExtractor.ts"
      via: "provides text for extraction"
      pattern: "export.*parsePDF"
---

<objective>
Build PDF upload and job extraction infrastructure: PDF parsing, text extraction, and job posting detection.

Purpose: Enable the system to accept PDF documents and extract structured job data from them. This is the foundation for PDF-based job detection that integrates with existing matching and email systems.

Output: PDF parser utility, job extractor logic, TypeScript types for PDF processing
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-pdf-job-detection/05-CONTEXT.md
@src/types/job.ts
@src/matching/scorer.ts
</context>

<interfaces>
<!-- These types will be created by this plan -->
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Define PDF processing types</name>
<files>src/types/pdf.ts</files>
<action>Create TypeScript types for PDF processing:
- PDFUploadResult: { success: boolean, text: string, pageCount: number, error?: string }
- ExtractedJob: { title: string, company: string, description: string, requirements: string[], location?: string, url?: string }
- PDFProcessingOptions: { minConfidence?: number, extractRequirements: boolean }

These types define the contract between PDF parsing and job extraction layers.
</action>
<verify>
<automated>npx tsc --noEmit src/types/pdf.ts</automated>
</verify>
<done>Type definitions compile without errors, all types exported</done>
</task>

<task type="auto">
<name>Task 2: Implement PDF parser with pdf-parse</name>
<files>src/lib/pdf/pdfParser.ts</files>
<action>Create PDF parsing utility using pdf-parse library:
- Install dependency: npm install pdf-parse
- Import pdf-parse and fs modules
- Export async function parsePDF(buffer: Buffer): Promise<PDFUploadResult>
- Use pdf-parse to extract text from PDF buffer
- Handle multi-page PDFs (concatenate all pages)
- Clean extracted text (remove extra whitespace, normalize line breaks)
- Return { success, text, pageCount, error }

Per 05-CONTEXT.md: Focus on reliable text extraction from job posting PDFs.
</action>
<verify>
<automated>npx tsc --noEmit src/lib/pdf/pdfParser.ts</automated>
</verify>
<done>PDF parser compiles, exports parsePDF function, handles Buffer input</done>
</task>

<task type="auto">
<name>Task 3: Create job posting extractor from text</name>
<files>src/lib/pdf/jobExtractor.ts</files>
<action>Implement job extraction logic:
- Export function extractJobsFromText(text: string, options?: PDFProcessingOptions): Promise<ExtractedJob[]>
- Split text into potential job sections (look for patterns like job titles, company names)
- Use regex patterns to identify:
  * Job titles (lines with common title patterns)
  * Company names (lines following "at" or standalone capitalized names)
  * Descriptions (paragraphs after title/company)
  * Requirements (bullet points, "requirements:", "qualifications:" sections)
- Return array of ExtractedJob objects

Heuristics: Job postings typically have title, company, description block, requirements list.
</action>
<verify>
<automated>npx tsc --noEmit src/lib/pdf/jobExtractor.ts</automated>
</verify>
<done>Job extractor compiles, exports extractJobsFromText function</done>
</task>
</tasks>

<verification>
All PDF processing files compile without TypeScript errors. Types are properly defined and imported. PDF parser can extract text from Buffer input. Job extractor identifies job sections from plain text.
</verification>

<success_criteria>
1. TypeScript compilation passes for all three files
2. PDFUploadResult type has 4 fields (success, text, pageCount, error?)
3. parsePDF function accepts Buffer and returns Promise<PDFUploadResult>
4. extractJobsFromText function accepts string and returns Promise<ExtractedJob[]>
5. Job extraction uses regex patterns for title, company, description, requirements
6. All files use ES module exports
</success_criteria>

<output>
After completion, create `.planning/phases/05-pdf-job-detection/05-01-SUMMARY.md`
</output>
