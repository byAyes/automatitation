---
phase: 04-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/job-email-automation.yml
  - src/jobs/history.ts
  - src/jobs/history.db
autonomous: true
requirements:
  - JOB-08
  - JOB-09
user_setup: []
must_haves:
  truths:
    - "Workflow runs automatically every week"
    - "Only new jobs are included in each digest"
  artifacts:
    - path: ".github/workflows/job-email-automation.yml"
      provides: "GitHub Actions workflow for weekly execution"
      contains: "schedule: \n    - cron: '0 0 * * 1'"
    - path: "src/jobs/history.ts"
      provides: "Job history management system"
      exports: ["JobHistory", "addJob", "isJobKnown"]
    - path: "src/jobs/history.db"
      provides: "Persistent job history database"
      contains: "SQLite database"
  key_links:
    - from: ".github/workflows/job-email-automation.yml"
      to: "src/jobs/history.ts"
      via: "calls job history module"
      pattern: "node src/jobs/history.ts"
    - from: "src/jobs/history.ts"
      to: "src/jobs/scraper.ts"
      via: "filters jobs before sending"
      pattern: "isJobKnown" 
    - from: "src/jobs/history.ts"
      to: "src/email/digest.ts"
      via: "provides filtered job list"
      pattern: "addJob"
---
<objective>
Create GitHub Actions workflow and job history system to enable automated weekly job email digests
Purpose: This phase enables the entire system to run automatically without manual intervention, ensuring users receive fresh job recommendations every week without duplicates. This is the final piece that transforms the system from a manual tool to a fully automated service.
Output: GitHub Actions workflow file, job history management system with persistent storage
</objective>
<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>
<context>
@.planning/ROADMAP.md
@.planning/phases/01-*/01-*.SUMMARY.md
@.planning/phases/02-*/02-*.SUMMARY.md
@.planning/phases/03-*/03-*.SUMMARY.md
</context>
<tasks>
<task type="auto">
<name>Task 1: Create GitHub Actions workflow for weekly execution</name>
<files>.github/workflows/job-email-automation.yml</files>
<action>Create GitHub Actions workflow that runs every Monday at midnight (0 0 * * 1). The workflow should:
1. Checkout the repository
2. Set up Node.js 20.x
3. Install dependencies with npm ci
4. Run the job automation script: node src/jobs/runner.js
5. Use the default GitHub token for authentication
6. Set a timeout of 10 minutes
7. Include appropriate error handling and notifications

Use the following structure:
- name: Job Email Automation
- on:
    schedule:
      - cron: '0 0 * * 1'
- jobs:
    run-automation:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-node@v4
          with:
            node-version: '20'
        - name: Install dependencies
          run: npm ci --only=production
        - name: Run job automation
          run: node src/jobs/runner.js
        - name: Handle failures
          if: failure()
          run: echo "Automation failed"</action>
<verify>
<automated>ls .github/workflows/job-email-automation.yml && grep -q "cron: '0 0 * * 1'" .github/workflows/job-email-automation.yml</automated>
</verify>
<done>GitHub Actions workflow file exists with weekly schedule trigger and all required steps</done>
</task>
<task type="auto">
<name>Task 2: Implement job history management system</name>
<files>src/jobs/history.ts</files>
<action>Create a JobHistory class in src/jobs/history.ts that:
1. Uses SQLite database stored at src/jobs/history.db
2. Has methods:
   - addJob(jobId: string): Promise<void> - adds job ID to database
   - isJobKnown(jobId: string): Promise<boolean> - checks if job ID exists
   - initialize(): Promise<void> - creates database and table if not exists
3. The table schema: CREATE TABLE IF NOT EXISTS job_history (id TEXT PRIMARY KEY, added_at INTEGER)
4. Uses sqlite3 npm package with proper error handling
5. Exports the JobHistory class as the default export
6. Includes type definitions for TypeScript

Use the following structure:
- Import sqlite3 package
- Define JobHistory class with constructor taking database path
- Implement initialize() method that creates table
- Implement addJob() and isJobKnown() methods
- Export JobHistory as default

Use proper async/await patterns and error handling for database operations.</action>
<verify>
<automated>node -e "require('fs').existsSync('src/jobs/history.ts')" && grep -q "class JobHistory" src/jobs/history.ts && grep -q "sqlite3" src/jobs/history.ts</automated>
</verify>
<done>Job history management system implemented with SQLite database, proper methods, and TypeScript types</done>
</task>
<task type="auto">
<name>Task 3: Update job runner to use history system</name>
<files>src/jobs/runner.js</files>
<action>Modify src/jobs/runner.js to:
1. Import the JobHistory class from src/jobs/history.ts
2. Initialize the job history system on startup
3. Before sending any job in the digest, check if it's already known using isJobKnown()
4. Only include new jobs in the email digest
5. After sending an email with new jobs, add all those job IDs to the history using addJob()
6. Log the number of new jobs found and sent
7. Maintain existing functionality for scraping and email sending

Update the code to use the new JobHistory system for duplicate detection and filtering.</action>
<verify>
<automated>node -e "require('fs').existsSync('src/jobs/runner.js')" && grep -q "JobHistory" src/jobs/runner.js && grep -q "isJobKnown" src/jobs/runner.js && grep -q "addJob" src/jobs/runner.js</automated>
</verify>
<done>Job runner uses job history system to filter out duplicates and only send new jobs</done>
</task>
</tasks>
<verification>
1. Verify GitHub Actions workflow exists in .github/workflows/job-email-automation.yml
2. Verify job history system exists in src/jobs/history.ts with SQLite database
3. Verify runner.js uses history system to filter duplicates
4. Manual test: Run runner.js locally and confirm it only sends new jobs
</verification>
<success_criteria>
1. GitHub Actions workflow runs automatically every Monday at midnight
2. Job email digests contain only new jobs (no duplicates)
3. Job history database persists between runs
4. System handles errors gracefully (no crashes on database issues)
</success_criteria>
<output>
After completion, create .planning/phases/04-automation/04-01-SUMMARY.md
</output>