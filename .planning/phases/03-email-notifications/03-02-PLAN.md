---
phase: 03-email-notifications
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/email/template.ts
  - src/app/api/email/send/route.ts
  - src/lib/email/gmail.ts
autonomous: false
requirements:
  - JOB-07
must_haves:
  truths:
    - "Email contains formatted job list"
    - "Email includes job title, company, location, and link"
    - "Email format is simple and readable"
    - "Jobs are sorted by match score"
  artifacts:
    - path: "src/lib/email/template.ts"
      provides: "Email template function for job digest"
      exports: ["formatJobDigest"]
    - path: "src/app/api/email/send/route.ts"
      provides: "Updated endpoint using template"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/app/api/email/send/route.ts"
      to: "src/lib/email/template.ts"
      via: "import formatJobDigest"
      pattern: "formatJobDigest.*from.*template"
    - from: "src/lib/email/gmail.ts"
      to: "src/lib/email/template.ts"
      via: "email body formatting"
      pattern: "formatJobDigest"
---

<objective>
Create email template for job digest and integrate with send endpoint

Purpose: Format matched jobs into a clean, readable email digest

Output: Email template module and integrated send functionality
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-email-notifications/03-CONTEXT.md
@.planning/phases/03-email-notifications/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- From Plan 03-01 -->
From src/lib/email/gmail.ts:
```typescript
export async function sendEmail(to: string, subject: string, body: string): Promise<SendResult>
```

From prisma schema (Phase 2):
```typescript
interface Job {
  id: string
  title: string
  company: string
  location: string | null
  url: string
  salary: number | null
  skills: string[]
}

interface MatchScore {
  jobId: string
  userId: string
  score: number
  matchedSkills: string[]
}
```
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Create email template function</name>
<files>src/lib/email/template.ts</files>
<action>
Create email template module for job digest formatting:

1. Export function `formatJobDigest(jobs, date)`:
   - Accepts array of jobs with match scores
   - Accepts date string for email header
   - Returns formatted string for email body

2. Template format (per CONTEXT.md):
   ```
   Subject: Weekly Job Digest - {date}

   Hi,

   Here are your matched jobs for this week:

   1. {Job Title} at {Company} - {Location}
      {Link}
      Match Score: {score}%
      Skills: {matched skills}

   2. ...

   Best regards,
   Job Email Automation
   ```

3. Sort jobs by match score (highest first)
4. Handle empty job list gracefully
5. Keep formatting simple (plain text, no HTML)
</action>
<verify>
<automated>npx tsc --noEmit src/lib/email/template.ts</automated>
</verify>
<done>Template module compiles, exports formatJobDigest function</done>
</task>

<task type="auto">
<name>Task 2: Integrate template with send endpoint</name>
<files>src/app/api/email/send/route.ts</files>
<action>
Update the email send endpoint to use the template:

1. Import formatJobDigest from template.ts
2. Update POST handler to accept optional job list
3. If jobs provided, format using formatJobDigest()
4. If no jobs, use provided subject/body (for testing)
5. Query matched jobs from database (Prisma) if not provided in request
6. Filter jobs by date range (last 7 days for weekly digest)
7. Sort by match score before formatting

Per D-01 (simple format): Keep email as plain text, no HTML.
</action>
<verify>
<automated>npx tsc --noEmit src/app/api/email/send/route.ts</automated>
</verify>
<done>Send endpoint updated to use template, compiles without errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 3: Verify email sending end-to-end</name>
<what-built>Complete email sending flow with Gmail API and job digest template</what-built>
<how-to-verify>
1. Ensure .env has Gmail OAuth credentials (GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, etc.)
2. If no tokens yet, run OAuth flow:
   - Start dev server: `npm run dev`
   - Visit http://localhost:3000/api/email/send
   - Follow OAuth consent flow
   - Save tokens to .env
3. Test sending:
   - Run: `curl -X POST http://localhost:3000/api/email/send -H "Content-Type: application/json" -d '{"to":"your_email@gmail.com","subject":"Test","body":"Test email"}'`
   - Check email inbox for test message
4. Verify email content:
   - Email received successfully
   - Subject line correct
   - Body content matches
   - No formatting issues
</how-to-verify>
<resume-signal>Type "approved" if email sent successfully, or describe issues</resume-signal>
</task>
</tasks>

<verification>
Overall phase checks:
1. Email template created with proper formatting
2. Send endpoint integrated with template
3. End-to-end email sending verified
4. Jobs sorted by match score in digest
</verification>

<success_criteria>
- [x] src/lib/email/template.ts created with formatJobDigest function
- [x] Send endpoint uses template for job digest
- [x] Email template produces readable, simple format
- [x] Jobs sorted by match score (descending)
- [x] Human verification passed (email received and formatted correctly)
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-notifications/03-02-SUMMARY.md`
</output>
