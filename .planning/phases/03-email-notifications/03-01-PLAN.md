---
phase: 03-email-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/lib/email/gmail.ts
  - src/app/api/email/send/route.ts
  - .env.example
autonomous: true
requirements:
  - JOB-06
must_haves:
  truths:
    - "User can send test email via API endpoint"
    - "Email is authenticated via OAuth2"
    - "Gmail API client is properly configured"
  artifacts:
    - path: "src/lib/email/gmail.ts"
      provides: "Gmail API client with OAuth2 authentication"
      exports: ["sendEmail", "authenticateGmail"]
    - path: "src/app/api/email/send/route.ts"
      provides: "API endpoint to send test email"
      exports: ["GET", "POST"]
  key_links:
    - from: "src/app/api/email/send/route.ts"
      to: "src/lib/email/gmail.ts"
      via: "import sendEmail"
      pattern: "sendEmail.*from.*gmail"
---

<objective>
Set up Gmail API integration with OAuth2 authentication

Purpose: Enable sending emails through Gmail API with proper authentication

Output: Working Gmail API client library and test endpoint
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-email-notifications/03-CONTEXT.md
@.planning/phases/03-email-notifications/03-RESEARCH.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Key types and contracts from existing codebase -->
<!-- From Phase 2 - Job and UserProfile types -->
From prisma/schema.prisma:
```prisma
model UserProfile {
  id              String  @id @default(uuid())
  userId          String  @unique
  skills          String[]
  interests       String[]
  location        String?
  remoteOnly      Boolean @default(false)
  experienceLevel String?
  minSalary       Int?
  maxSalary       Int?
}

model Job {
  id          String   @id @default(uuid())
  title       String
  company     String
  location    String?
  description String?
  url         String   @unique
  salary      Int?
  postedAt    DateTime?
  skills      String[]
  category    String?
}
```
</interfaces>

<tasks>
<task type="auto">
<name>Task 1: Install Gmail API dependencies</name>
<files>package.json</files>
<action>
Install required packages for Gmail API integration:
- `npm install googleapis google-auth-library`
These provide the official Gmail API client and OAuth2 authentication support.

Also install types for development:
- `npm install -D @types/node` (if not already present)
</action>
<verify>
<automated>npm list googleapis google-auth-library</automated>
</verify>
<done>Packages installed and listed in package.json dependencies</done>
</task>

<task type="auto">
<name>Task 2: Create Gmail API client library</name>
<files>src/lib/email/gmail.ts</files>
<action>
Create Gmail API client module with OAuth2 authentication:

1. Import googleapis and OAuth2Client
2. Initialize OAuth2 client with env vars (CLIENT_ID, CLIENT_SECRET, REDIRECT_URI)
3. Export function to generate auth URL for initial OAuth flow
4. Export function to exchange auth code for tokens
5. Export sendEmail function that:
   - Creates RFC 2822 formatted email (subject, body, from, to)
   - Base64 encodes the message
   - Calls gmail.messages.send() with authenticated client
6. Handle token refresh automatically via google-auth-library

Per research: Use google.auth.OAuth2, set as global auth, handle base64 encoding properly.
</action>
<verify>
<automated>npx tsc --noEmit src/lib/email/gmail.ts</automated>
</verify>
<done>Gmail module compiles without errors, exports sendEmail and auth functions</done>
</task>

<task type="auto">
<name>Task 3: Create test email API endpoint</name>
<files>src/app/api/email/send/route.ts</files>
<action>
Create Next.js App Router API endpoint for sending test emails:

1. Create POST handler that:
   - Accepts {to, subject, body} in request body
   - Calls sendEmail() from gmail.ts
   - Returns {success: true, messageId: string} on success
   - Returns {error: string} on failure with proper HTTP status codes
2. Create GET handler for testing that sends a test email to configured address
3. Add error handling for OAuth errors, network errors, Gmail API errors
4. Log all send attempts for debugging
</action>
<verify>
<automated>npx tsc --noEmit src/app/api/email/send/route.ts</automated>
</verify>
<done>API endpoint compiles, handles POST requests, returns proper responses</done>
</task>

<task type="auto">
<name>Task 4: Add environment variable examples</name>
<files>.env.example</files>
<action>
Create or update .env.example with required Gmail API variables:

```
# Gmail API OAuth2 Credentials
GOOGLE_CLIENT_ID=your_client_id_here
GOOGLE_CLIENT_SECRET=your_secret_here
GOOGLE_REDIRECT_URI=http://localhost:3000/oauth2callback
GMAIL_RECIPIENT=your_email@example.com

# OAuth2 tokens (populated after first auth)
GMAIL_ACCESS_TOKEN=
GMAIL_REFRESH_TOKEN=
```

Document where to get these values (Google Cloud Console).
</action>
<verify>
<automated>test -f .env.example && grep -q "GOOGLE_CLIENT_ID" .env.example</automated>
</verify>
<done>.env.example file exists with all required Gmail API variables documented</done>
</task>
</tasks>

<verification>
Overall phase checks:
1. All dependencies installed successfully
2. Gmail API client module created with proper OAuth2 flow
3. Test endpoint created and compiles
4. Environment variables documented
5. TypeScript compilation passes for all new files
</verification>

<success_criteria>
- [x] googleapis and google-auth-library installed
- [x] src/lib/email/gmail.ts created with sendEmail function
- [x] src/app/api/email/send/route.ts created with GET/POST handlers
- [x] .env.example updated with Gmail API credentials
- [x] All TypeScript files compile without errors
- [x] Test endpoint can be called (manual verification in next plan)
</success_criteria>

<output>
After completion, create `.planning/phases/03-email-notifications/03-01-SUMMARY.md`
</output>
