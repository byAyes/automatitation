---
phase: 06-cv-database
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [prisma/schema.prisma, src/types/cv.ts, src/app/api/cv/upload/route.ts]
autonomous: true
requirements: [CV-01, CV-05]
must_haves:
  truths:
    - "User can upload CV PDF via API"
    - "CV is stored with version number and timestamp"
    - "Multiple CV versions are linked to same user"
    - "User can retrieve all their CV versions"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "CV model with versioning"
      contains: "model CV"
    - path: "src/types/cv.ts"
      provides: "CV upload and version types"
      exports: ["CV", "CVUploadResult", "CVVersion"]
    - path: "src/app/api/cv/upload/route.ts"
      provides: "CV upload endpoint"
      exports: ["POST", "GET"]
  key_links:
    - from: "src/app/api/cv/upload/route.ts"
      to: "prisma.cV"
      via: "database save"
      pattern: "prisma\\.cV\\.create"
    - from: "src/app/api/cv/upload/route.ts"
      to: "src/lib/cv/cvParser.ts"
      via: "import"
      pattern: "import.*cvParser"
---

<objective>
Create CV upload API with database storage and versioning support.

Purpose: Enable users to upload CV PDFs that are stored with version tracking, forming the foundation for auto-extraction and profile updates.

Output: CV model with versioning, upload endpoint, version history retrieval.
</objective>

<execution_context>
@C:\Users\juans\Documents\job-email-automation\.planning\phases\05-pdf-job-detection\PHASE-SUMMARY.md
@C:\Users\juans\Documents\job-email-automation\.planning\phases\02-ai-job-matching\02-01-SUMMARY.md
</execution_context>

<context>
@C:\Users\juans\Documents\job-email-automation\prisma\schema.prisma
@C:\Users\juans\Documents\job-email-automation\src\types\pdf.ts
@C:\Users\juans\Documents\job-email-automation\src\types\user-profile.ts

<interfaces>
<!-- Key types and contracts from existing codebase -->
<!-- From prisma/schema.prisma - UserProfile model -->
```prisma
model UserProfile {
  id String @id @default(uuid())
  userId String @unique
  skills String[]
  interests String[]
  location String?
  remoteOnly Boolean @default(false)
  experienceLevel String?
  minSalary Int?
  maxSalary Int?
  skillWeight Float @default(0.4)
  interestWeight Float @default(0.3)
  locationWeight Float @default(0.2)
  salaryWeight Float @default(0.1)
}
```

<!-- From src/types/pdf.ts - PDF upload result -->
```typescript
export interface PDFUploadResult {
  success: boolean;
  text: string;
  pageCount: number;
  error?: string;
}
```
</interfaces>
</context>

<tasks>
<task type="auto">
<name>Task 1: Add CV model with versioning to Prisma schema</name>
<files>prisma/schema.prisma</files>
<action>
Add CV model to schema.prisma with the following fields:
- id: String @id @default(uuid())
- userId: String (references UserProfile)
- version: Int (starts at 1, increments with each upload)
- fileUrl: String (storage path)
- fileName: String (original filename)
- fileSize: Int (bytes)
- uploadedAt: DateTime @default(now())
- skills: String[] (extracted skills snapshot)
- experience: String[] (extracted experience entries)
- education: String[] (extracted education entries)
- rawText: String? (full extracted text for re-processing)
- status: String @default("pending") (pending, processed, failed)

Add relation to UserProfile:
- cvs: CV[] (in UserProfile model)

Ensure unique constraint on (userId, version) for version tracking.
</action>
<verify>
<automated>npx prisma generate</automated>
</verify>
<done>
Prisma schema updated with CV model, migration created, TypeScript types generated.
</done>
</task>

<task type="auto">
<name>Task 2: Create CV TypeScript types</name>
<files>src/types/cv.ts</files>
<action>
Create src/types/cv.ts with:
1. CV interface matching Prisma CV model exactly
2. CVUploadResult interface for API responses:
   - success: boolean
   - cvId?: string
   - version?: number
   - fileUrl?: string
   - error?: string
3. CVVersion interface for version history:
   - version: number
   - uploadedAt: Date
   - fileName: string
   - fileSize: number
   - status: string
   - skillsCount: number
4. CVUploadInput interface for API requests:
   - userId: string
   - file: Buffer
   - fileName: string
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>
TypeScript types compile without errors, CV types exported from src/types/cv.ts
</done>
</task>

<task type="auto">
<name>Task 3: Create CV upload API endpoint</name>
<files>src/app/api/cv/upload/route.ts</files>
<action>
Create POST /api/cv/upload endpoint:
1. Accept multipart/form-data or binary PDF with userId
2. Validate file type (PDF only) and size (<10MB)
3. Save file to /public/cvs/${userId}/${filename}
4. Calculate next version number (max version + 1)
5. Create CV record in database with status="pending"
6. Return { success, cvId, version, fileUrl }

Create GET /api/cv/versions?userId=<id> endpoint:
1. Fetch all CV versions for user
2. Return sorted by version (descending)
3. Return { versions: CVVersion[] }

Use existing pdf-parse library for initial text extraction (rawText field).
</action>
<verify>
<automated>curl -X GET http://localhost:3000/api/cv/versions?userId=test -w "\nHTTP Status: %{http_code}\n"</automated>
</verify>
<done>
POST endpoint accepts CV PDF and returns version number, GET endpoint returns version history
</done>
</task>
</tasks>

<verification>
1. Upload test CV PDF → returns version 1
2. Upload same user's second CV → returns version 2
3. GET /api/cv/versions returns both versions with timestamps
4. Database contains CV records with correct userId and version
</verification>

<success_criteria>
- [ ] CV model exists in Prisma schema with all 12 fields
- [ ] CV model has relation to UserProfile
- [ ] POST /api/cv/upload accepts PDF and returns version number
- [ ] GET /api/cv/versions returns version history
- [ ] File stored in /public/cvs/ directory
- [ ] Version numbers increment correctly per user
</success_criteria>

<output>
After completion, create `.planning/phases/06-cv-database/06-01-SUMMARY.md`
</output>
