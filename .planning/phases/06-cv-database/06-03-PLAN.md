---
phase: 06-cv-database
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: [src/lib/cv/profileHistory.ts, src/app/api/profile/history/route.ts, src/matching/cvMatcher.ts]
autonomous: false
requirements: [CV-04, CV-05]
must_haves:
  truths:
    - "User can view profile change history"
    - "Job scraping uses updated profile for matching"
    - "Profile changes are timestamped"
    - "CV-triggered updates are marked"
  artifacts:
    - path: "src/lib/cv/profileHistory.ts"
      provides: "Profile change tracking"
      exports: ["trackProfileChange", "getProfileHistory"]
    - path: "src/app/api/profile/history/route.ts"
      provides: "History retrieval endpoint"
      exports: ["GET"]
    - path: "src/matching/cvMatcher.ts"
      provides: "CV-based job matching"
      exports: ["matchJobsToCVProfile"]
  key_links:
    - from: "src/matching/cvMatcher.ts"
      to: "src/matching/scorer.ts"
      via: "import and reuse"
      pattern: "import.*scoreAndSortJobs"
    - from: "src/app/api/cv/update-profile/route.ts"
      to: "src/lib/cv/profileHistory.ts"
      via: "import"
      pattern: "import.*trackProfileChange"
---

<objective>
Track profile changes over time and integrate updated profile with job scraping/matching.

Purpose: Enable users to see how their profile evolved from CV uploads, and ensure job scraping uses the latest profile data.

Output: Profile history tracking, change timeline API, CV-enhanced job matching.
</objective>

<execution_context>
@C:\Users\juans\Documents\job-email-automation\.planning\phases\05-pdf-job-detection\PHASE-SUMMARY.md
@C:\Users\juans\Documents\job-email-automation\.planning\phases\02-ai-job-matching\02-02-SUMMARY.md
</execution_context>

<context>
@C:\Users\juans\Documents\job-email-automation\src\types\user-profile.ts
@C:\Users\juans\Documents\job-email-automation\src\matching\scorer.ts
@C:\Users\juans\Documents\job-email-automation\prisma\schema.prisma

<interfaces>
<!-- From src/matching/scorer.ts - existing matching logic -->
```typescript
// Reuse scoreAndSortJobs function for CV-based matching
export function scoreAndSortJobs(jobs: Job[], profile: UserProfile): MatchedJob[];
```

<!-- UserProfile structure -->
```typescript
export interface UserProfile {
  id: string;
  userId: string;
  skills: string[];
  interests: string[];
  location: string | null;
  remoteOnly: boolean;
  experienceLevel: ExperienceLevel | null;
  minSalary: number | null;
  maxSalary: number | null;
}
```
</interfaces>
</context>

<tasks>
<task type="auto">
<name>Task 1: Create profile change tracking library</name>
<files>src/lib/cv/profileHistory.ts</files>
<action>
Create src/lib/cv/profileHistory.ts:

1. Add ProfileChangeLog model to prisma/schema.prisma:
   - id: String @id @default(uuid())
   - userId: String
   - changeType: String (skills_added, skills_removed, experience_updated, etc.)
   - previousValue: String? (JSON stringified)
   - newValue: String (JSON stringified)
   - source: String (cv_upload, manual, system)
   - cvId: String? (if triggered by CV)
   - createdAt: DateTime @default(now())

2. trackProfileChange(changes: ProfileChangeInput): Promise<void>
   - Save change log entries to database
   - Include timestamp, source, cvId reference

3. getProfileHistory(userId: string, limit?: number): Promise<ProfileChangeLog[]>
   - Fetch change history for user
   - Sort by createdAt descending
   - Return limited results
</action>
<verify>
<automated>npx prisma generate</automated>
</verify>
<done>
ProfileChangeLog model added, tracking functions implemented
</done>
</task>

<task type="auto">
<name>Task 2: Create profile history API endpoint</name>
<files>src/app/api/profile/history/route.ts</files>
<action>
Create GET /api/profile/history?userId=<id>&limit=50 endpoint:

1. Validate userId parameter
2. Fetch profile change logs from database
3. Include related CV info if cvId present
4. Return paginated history:
   ```json
   {
     "history": [
       {
         "id": "change-id",
         "changeType": "skills_added",
         "timestamp": "2026-03-28T10:00:00Z",
         "source": "cv_upload",
         "changes": {
           "added": ["react", "typescript"],
           "removed": []
         },
         "cvId": "cv-123"
       }
     ],
     "total": 10
   }
   ```

Create GET /api/profile/current?userId=<id> endpoint:
1. Fetch current UserProfile
2. Return full profile object
</action>
<verify>
<automated>curl "http://localhost:3000/api/profile/history?userId=test-user&limit=10" -w "\nHTTP Status: %{http_code}\n"</automated>
</verify>
<done>
GET endpoint returns timestamped profile change history
</done>
</task>

<task type="auto">
<name>Task 3: Integrate CV-updated profile with job matching</name>
<files>src/matching/cvMatcher.ts</files>
<action>
Create src/matching/cvMatcher.ts:

1. matchJobsToCVProfile(jobs: Job[], cvId: string): Promise<MatchedJob[]>
   - Fetch CV data (skills, experience, education)
   - Build temporary UserProfile from CV data
   - Call existing scoreAndSortJobs() from src/matching/scorer.ts
   - Return scored and sorted jobs

2. Update src/app/api/cv/update-profile/route.ts:
   - After updating UserProfile, call trackProfileChange()
   - Log source="cv_upload" and cvId
   - Track which fields changed (skills, experienceLevel, location)

3. Ensure job scraping (Phase 1 scrapers) use updated profile:
   - Scrapers fetch UserProfile before filtering
   - Use latest profile skills for matching
   - No code changes needed if already using scorer.ts
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>
CV data integrated with job matching, profile changes tracked
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
<name>Task 4: Verify end-to-end CV upload and profile update flow</name>
<what-built>
Complete CV upload → extraction → profile update → job matching pipeline
</what-built>
<how-to-verify>
1. Upload a CV PDF: POST /api/cv/upload
2. Process CV: POST /api/cv/process
3. Update profile: POST /api/cv/update-profile
4. Verify UserProfile.skills contains extracted skills
5. Check profile history: GET /api/profile/history
6. Run job matching: GET /api/match-jobs
7. Verify matched jobs use updated skills from CV
</how-to-verify>
<resume-signal>
Type "approved" if all steps work, or describe issues found
</resume-signal>
</task>
</tasks>

<verification>
1. Profile history endpoint returns change log entries
2. CV upload triggers profile change tracking
3. Job matching uses updated profile from CV
4. History shows source="cv_upload" with cvId reference
5. Skills from CV appear in job matching criteria
</verification>

<success_criteria>
- [ ] ProfileChangeLog model in database
- [ ] trackProfileChange() logs all profile modifications
- [ ] GET /api/profile/history returns timestamped changes
- [ ] History includes source (cv_upload vs manual)
- [ ] Job matching uses CV-updated profile
- [ ] CV-triggered changes linked to CV record
- [ ] User can see what changed and when
</success_criteria>

<output>
After completion, create `.planning/phases/06-cv-database/06-03-SUMMARY.md`
</output>
