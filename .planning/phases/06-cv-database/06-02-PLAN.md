---
phase: 06-cv-database
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified: [src/lib/cv/cvParser.ts, src/lib/cv/skillExtractor.ts, src/app/api/cv/process/route.ts, src/app/api/cv/update-profile/route.ts]
autonomous: true
requirements: [CV-02, CV-03]
must_haves:
  truths:
    - "System extracts skills from CV text"
    - "System extracts work experience from CV"
    - "System extracts education from CV"
    - "UserProfile is updated with extracted data"
  artifacts:
    - path: "src/lib/cv/cvParser.ts"
      provides: "CV text parsing and section detection"
      exports: ["parseCV"]
    - path: "src/lib/cv/skillExtractor.ts"
      provides: "Skill and experience extraction logic"
      exports: ["extractSkills", "extractExperience", "extractEducation"]
    - path: "src/app/api/cv/process/route.ts"
      provides: "CV processing endpoint"
      exports: ["POST"]
  key_links:
    - from: "src/lib/cv/skillExtractor.ts"
      to: "src/matching/scorer.ts"
      via: "skill matching patterns"
      pattern: "import.*scorer"
    - from: "src/app/api/cv/update-profile/route.ts"
      to: "prisma.userProfile"
      via: "database update"
      pattern: "prisma\\.userProfile\\.update"
---

<objective>
Implement CV parsing to auto-extract skills, experience, and education, then update UserProfile automatically.

Purpose: Transform uploaded CV PDFs into structured profile updates, eliminating manual profile maintenance.

Output: CV parser, skill extractor, profile update endpoint.
</objective>

<execution_context>
@C:\Users\juans\Documents\job-email-automation\.planning\phases\05-pdf-job-detection\PHASE-SUMMARY.md
@C:\Users\juans\Documents\job-email-automation\.planning\phases\02-ai-job-matching\02-01-SUMMARY.md
</execution_context>

<context>
@C:\Users\juans\Documents\job-email-automation\src\types\user-profile.ts
@C:\Users\juans\Documents\job-email-automation\src\lib\pdf\pdfParser.ts
@C:\Users\juans\Documents\job-email-automation\src\lib\pdf\jobExtractor.ts

<interfaces>
<!-- From src/types/user-profile.ts - UserProfile structure -->
```typescript
export interface UserProfile {
  id: string;
  userId: string;
  skills: string[];
  interests: string[];
  location: string | null;
  remoteOnly: boolean;
  experienceLevel: ExperienceLevel | null;
  minSalary: number | null;
  maxSalary: number | null;
}
```

<!-- From src/lib/pdf/pdfParser.ts - PDF parsing pattern -->
```typescript
export interface PDFUploadResult {
  success: boolean;
  text: string;
  pageCount: number;
  error?: string;
}
```
</interfaces>
</context>

<tasks>
<task type="auto">
<name>Task 1: Create CV text parser with section detection</name>
<files>src/lib/cv/cvParser.ts</files>
<action>
Create src/lib/cv/cvParser.ts:
1. parseCV(pdfBuffer: Buffer): Promise<CVParsedResult>
   - Use pdf-parse library (existing dependency)
   - Extract raw text from PDF
   - Clean and normalize text (remove special chars, normalize whitespace)
   - Detect sections using regex patterns:
     * Skills/Technical Skills section
     * Experience/Work History section
     * Education section
   - Return { rawText, sections: { skills: string, experience: string, education: string } }
2. Use similar pattern to src/lib/pdf/jobExtractor.ts for section detection
3. Handle multi-page CVs (2-3 pages typical)
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>
CV parser extracts raw text and identifies section boundaries
</done>
</task>

<task type="auto">
<name>Task 2: Create skill and experience extractor</name>
<files>src/lib/cv/skillExtractor.ts</files>
<action>
Create src/lib/cv/skillExtractor.ts with three extraction functions:

1. extractSkills(text: string): string[]
   - Parse skills section text
   - Extract individual skills (split by commas, newlines, bullets)
   - Normalize skill names (lowercase, trim)
   - Filter common false positives
   - Return array of skill strings

2. extractExperience(text: string): ExperienceEntry[]
   - Parse experience section
   - Extract: jobTitle, company, duration, description
   - Use regex patterns for date ranges, company names
   - Return array of experience entries

3. extractEducation(text: string): EducationEntry[]
   - Parse education section
   - Extract: degree, institution, graduationYear
   - Return array of education entries

Reuse patterns from src/lib/pdf/jobExtractor.ts for regex extraction.
</action>
<verify>
<automated>npx tsc --noEmit</automated>
</verify>
<done>
Extractor functions parse CV sections into structured data
</done>
</task>

<task type="auto">
<name>Task 3: Create CV processing endpoint</name>
<files>src/app/api/cv/process/route.ts</files>
<action>
Create POST /api/cv/process endpoint:
1. Accept cvId parameter
2. Fetch CV record from database (status="pending")
3. Read PDF file from storage
4. Call parseCV() to extract raw text
5. Call extractSkills(), extractExperience(), extractEducation()
6. Update CV record with extracted data (skills, experience, education arrays)
7. Update CV status to "processed"
8. Return { success, skills: string[], experience: count, education: count }

This endpoint is called after upload, before profile update.
</action>
<verify>
<automated>curl -X POST http://localhost:3000/api/cv/process -H "Content-Type: application/json" -d '{"cvId":"test-id"}' -w "\nHTTP Status: %{http_code}\n"</automated>
</verify>
<done>
POST endpoint processes CV PDF and populates extracted data fields
</done>
</task>

<task type="auto">
<name>Task 4: Create profile auto-update endpoint</name>
<files>src/app/api/cv/update-profile/route.ts</files>
<action>
Create POST /api/cv/update-profile endpoint:
1. Accept userId and cvId parameters
2. Fetch processed CV data (skills, experience, education)
3. Fetch current UserProfile
4. Merge strategy (skills):
   - Combine existing skills with new skills
   - Remove duplicates (case-insensitive)
   - Update UserProfile.skills array
5. Update experienceLevel based on years of experience:
   - 0-2 years: junior
   - 3-5 years: mid
   - 6-10 years: senior
   - 10+ years: lead
6. Extract location from most recent experience if available
7. Update UserProfile with extracted data
8. Return { success, updatedFields: string[] }

Use Prisma transaction for atomic update.
</action>
<verify>
<automated>curl -X POST http://localhost:3000/api/cv/update-profile -H "Content-Type: application/json" -d '{"userId":"user-123","cvId":"cv-456"}' -w "\nHTTP Status: %{http_code}\n"</automated>
</verify>
<done>
POST endpoint updates UserProfile with extracted CV data, merges skills intelligently
</done>
</task>
</tasks>

<verification>
1. Upload CV → process → skills extracted to CV record
2. Call update-profile → UserProfile.skills contains extracted skills
3. Experience level calculated correctly from years
4. No duplicate skills in merged result
</verification>

<success_criteria>
- [ ] CV parser extracts text and detects sections
- [ ] Skills extracted as string array
- [ ] Experience parsed into structured entries
- [ ] Education parsed into structured entries
- [ ] POST /api/cv/process updates CV with extracted data
- [ ] POST /api/cv/update-profile updates UserProfile
- [ ] Skills merged without duplicates
- [ ] Experience level inferred from years
</success_criteria>

<output>
After completion, create `.planning/phases/06-cv-database/06-02-SUMMARY.md`
</output>
